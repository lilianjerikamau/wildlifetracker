{{#partial "content"}}

    <h1> List o' stuff to do!</h1>

    {{#if tasks}}
        {{#each tasks }}
            <p><a href="/tasks/{{id}}">{{name}}</a></p>
        {{/each}}
    {{else}}
        <p>No tasks!</p>
    {{/if}}
{{/partial}}

{{> layout.hbs}}